import React from "react";
import { Helmet } from "react-helmet";

const MetaData = ({ title }) => {
  return (
    <Helmet>
      <title>Trendz - An Ecommerce Site</title>
    </Helmet>
  );
};

export default MetaData;
